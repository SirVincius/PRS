@model (string ImageURL, string AltText)

<div class="button-container">
    <button class="img-container">
        <img class="img-fit" src="@Model.ImageURL" alt="@Model.AltText" />
    </button>
</div>

